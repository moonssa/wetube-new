extends base.pug

block content
    h4 Change title of video 
    form(method="POST")
        input(name="title" 
                placeholder="Video Title"
                value="" required)
        input(value="Save" type="submit")