extends base

block content 
    if errorMessage
        span=errorMessage
    form(method="POST")
        input(placeholder="Username",name="username" , type="text", required)
        input(placeholder="Password", name="password" , type="password", required)
        input(type="submit", value="LogIn")
        br
        a(href="https://github.com/login/oauth/authorize?client_id=2a77d4b2b1b6b7b9d8b1&allow_signup=false") Continue with github &rarr;
    hr
    div 
        span Don'y you have an account?     
        a(href="/join") join now  &rarr;